# The "build" workflow produces wheels (and the sdist) for all python
# versions/platforms. Where possible (i.e. the build is not a cross-compile),
# the test suite is also run for the wheel (this test covers fewer
# configurations than the "test" workflow and tox.ini).
name: Build

on:
  push:
    branches:
      # Run on release branches. This gives us a chance to detect rot in this
      # configuration before pushing a tag (which we'd rather not have to undo).
      - "branch[0-9]*"
    tags:
      # The main purpose of this workflow is to build wheels for release tags.
      # It runs automatically on tags matching this pattern and pushes to pypi.
      - "v*"
  workflow_dispatch:
    # Allow this workflow to be run manually (pushing to testpypi instead of pypi)

permissions: {}

env:
  # [[[cog cog.outl(f"python-version: &python-version '3.{default_python_minor}'")]]]
  python-version: &python-version '3.13'
  # [[[end]]]

jobs:
  build_sdist:
    name: Build sdist
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-python@v6
        name: Install Python
        with:
          python-version: ${{ env.python-version }}

      - name: Install dependencies
        run: python -m pip install setuptools
      - name: Check metadata
        run: "python setup.py check"
      - name: Build sdist
        run: "python setup.py sdist && ls -l dist"

      - uses: actions/upload-artifact@v7
        with:
          name: artifacts-sdist
          path: ./dist/tornado-*.tar.gz

  build_wheels:
    name: Build wheels on ${{ matrix.os }}${{ matrix.name_suffix || '' }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, ubuntu-24.04-arm, windows-2025, windows-11-arm, macos-15]
        emulated: [false]
        include:
          - os: ubuntu-24.04
            emulated: true
            arch: "riscv64"
            name_suffix: "-riscv64"
          - os: ubuntu-24.04
            emulated: true
            arch: "ppc64le"
            name_suffix: "-ppc64le"
          - os: ubuntu-24.04
            emulated: true
            arch: "s390x"
            name_suffix: "-s390x"

    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-python@v6
        name: Install Python
        with:
          python-version: ${{ env.python-version }}

      - name: Set up QEMU
        if: matrix.emulated
        uses: docker/setup-qemu-action@v4
        with:
          platforms: ${{ matrix.arch }}

      - name: Build wheels
        uses: pypa/cibuildwheel@v4.2.1
        env:
          CIBW_ARCHS: ${{ matrix.emulated && matrix.arch || '' }}
          # Increase timeouts for emulated archs
          CIBW_ENVIRONMENT: ${{ matrix.emulated && 'ASYNC_TEST_TIMEOUT=30 EMULATION=1' || '' }}

      - name: Audit ABI3 compliance
        # This may be moved into cibuildwheel itself in the future. See
        # https://github.com/pypa/cibuildwheel/issues/1342
        run: python -m pip install abi3audit && abi3audit --verbose --summary ./wheelhouse/*abi3*.whl

      - uses: actions/upload-artifact@v7
        with:
          name: artifacts-${{ matrix.os }}${{ matrix.name_suffix || '' }}
          path: ./wheelhouse/*.whl

  downstream:
    name: Downstream testbed
    needs: [build_wheels, build_sdist]
    # Only meaningful on the canonical repo; forks have no reason to spend an
    # hour of CI on downstream suites.
    if: github.repository == 'tornadoweb/tornado'
    # Runs the test suites of the ten most popular packages that depend on
    # tornado against the wheel this run just built. Because a called workflow
    # shares this run, the testbed downloads that artifact directly rather than
    # re-resolving a git ref -- so what the downstream suites import is exactly
    # what we are about to publish.
    #
    # Deliberately NOT in upload_pypi's `needs`: this is a signal to read before
    # pushing a tag, not a gate that a flaky downstream suite can use to wedge a
    # release. build.yml already runs on release-branch pushes, which is the
    # point at which there is still time to act on it.
    uses: tornadoweb/tornado-testbed/.github/workflows/testbed.yml@main
    with:
      tornado_artifact: artifacts-ubuntu-24.04
      python_version: *python-version
    permissions:
      contents: read

  upload_pypi_test:
    name: Upload to PyPI (test)
    needs: [build_wheels, build_sdist]
    runs-on: ubuntu-24.04
    if: github.repository == 'tornadoweb/tornado' && github.event_name == 'workflow_dispatch'
    permissions:
      # This permission is required for pypi's "trusted publisher" feature
      id-token: write
    steps:
      - uses: actions/download-artifact@v7
        with:
          pattern: artifacts-*
          path: dist
          merge-multiple: true

      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true

  upload_pypi:
    name: Upload to PyPI (prod)
    needs: [build_wheels, build_sdist]
    runs-on: ubuntu-24.04
    if: github.repository == 'tornadoweb/tornado' && github.event_name == 'push' && github.ref_type == 'tag' && startsWith(github.ref_name, 'v')
    permissions:
      # This permission is required for pypi's "trusted publisher" feature
      id-token: write
    steps:
      - uses: actions/download-artifact@v7
        with:
          pattern: artifacts-*
          path: dist
          merge-multiple: true

      - uses: pypa/gh-action-pypi-publish@release/v1
